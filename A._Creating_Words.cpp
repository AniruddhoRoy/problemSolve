#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define pr pair<int, int>
int main()
{
    int n;
    cin >> n;
    while (n--)
    {
        char a, b, c, x, y, z, i;
        cin >> a >> b >> c >> x >> y >> z;
        cout << x << b << c << " " << a << y << z << endl;
        ;
    }
    return 0;
}