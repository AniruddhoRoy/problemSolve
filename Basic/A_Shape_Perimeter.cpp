#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define pr pair<int, int>
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int m, n;
        cin >> n >> m;
        int result = n * m;
        int x = 0, y = 0;
        int p, q;
        for (int i = 0; i < n; i++)
        {
            cin >> x >> y;
        }
    }

    return 0;
}